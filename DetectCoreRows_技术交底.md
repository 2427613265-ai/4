# 岩芯箱行检测与回次划分算法技术交底

**交底对象：** 岩芯图像识别、岩芯箱排版解析、RQD 统计相关研发与现场实施人员  
**交底范围：** `DetectCoreRows.py` 中的行聚类、分段投影行判定、回次扫描划分及配套可视化  
**不在本次交底范围：** 岩芯检测模型本身（`ModelRun.runCoreModel_OutputCoreBoxCoor`）、透视校正、RQD 长度换算公式的现场标定

---

## 1. 交底目的

本模块解决的不是“图里有没有岩芯”，而是在**已经拿到岩芯多边形框**之后，把这些框整理成现场可使用的结构信息：

1. **行数判定：** 一块岩芯箱里有几排岩芯。
2. **行归属：** 每一段岩芯属于从上到下的第几行。
3. **回次划分：** 结合岩芯牌位置，把岩芯按“遇牌则结束当前回次、无牌则延续”的规则切成回次。
4. **过程留痕：** 把分段投票、末行不满判定、最终行中心画到图上，便于人工复核。

现场常见难点是隔板弯曲、末行岩芯不满箱、破碎段稀疏。全局只按 Y 坐标聚一次类，容易把相邻两行并成一行，或把噪声多判成一行。本方案用**竖条分段 + 投票 + 一次 KMeans 定稿**来压这类误判。

---

## 2. 输入、输出与坐标系约定

### 2.1 坐标系

- 图像原点在左上角，X 向右增大，Y 向下增大。
- “从上到下第 1 行”对应 **Y 更小** 的簇，程序内部行号从 0 开始。

### 2.2 输入

| 名称 | 形态 | 说明 |
|---|---|---|
| 岩芯框 `boxes` | `[[[x,y], ...], ...]` | 每个岩芯一段多边形。后续计算一律用顶点的 min/max，不要求必须是矩形。 |
| 图像宽高 | 像素整数 | 分段投影必须用整图宽高，不能只用框的外接范围代替。 |
| 透视校正图路径 | 可选 | 仅用于出调试图。没有路径时，行数计算仍可进行，只是不落盘可视化。 |
| 岩芯牌 `tag_boxes` | 回次划分阶段使用 | `__main__` 示例里是中心点 `[x, y]`；可视化阶段再扩成矩形 `[x1,y1,x2,y2]`。 |
| 检测模型 | `runCoreModel_OutputCoreBoxCoor(image_path)` | 入口脚本依赖该接口产出 `boxes`。本文件不实现模型。 |

### 2.3 输出

| 名称 | 含义 |
|---|---|
| `row_count` | 最终行数 N |
| `row_labels` | 每个岩芯框的行号，0 为最上行；DBSCAN 噪声为 -1 |
| `cluster_centers` | 每行中心 Y，已按从上到下排序 |
| `run_labels` / `run_info` | 回次编号及每行内的 X 区间、结束牌位置 |

---

## 3. 总体工艺流程

推荐主路径是**分段投影**。入口 `__main__` 目前走的是全局 DBSCAN，两者不要混用结果。

```
岩芯多边形框
    │
    ├─【方案 A · 全局 DBSCAN】仅按整图 Y 聚类，适合隔板较直、行间距稳定的箱子
    │
    └─【方案 B · 分段投影，主推荐】
          ① 沿 X 切成 10 条竖带
          ② 每个多边形按竖带边界做 X 方向裁剪（不是按中心点扔进某一段）
          ③ 每段独立 DBSCAN，得到该段候选行数和簇中心
          ④ 各段行数投票，众数作为基准 N；众数并列时取较小值
          ⑤ 若第 0 段检出 N+1，暂定“末行不满”，候选行数改为 N+1
          ⑥ 用第 0 段多出来的那一行，与第 1～4 段中“恰好 N 行”的末行做 Y 差校验
             · 差值过小 → 判定第 0 段误检，撤销 +1
             · 差值足够大 → 保留 N+1
          ⑦ 按已确定的最终行数，只做一次 KMeans，完成全框行归属
          ⑧ 如提供底图，输出分段调试图、必要时输出末行校验图
    │
    ▼
行标签 + 行中心
    │
    ▼
结合岩芯牌，逐行从左到右扫描划分回次
    │
    ▼
回次标签、回次区间、可视化
```

**定稿原则：** 行数必须在分段 DBSCAN 原始结果上先确定，再做唯一一次 KMeans。禁止“先按 N+1 聚、发现错了再按 N 重聚”。

---

## 4. 基础行聚类（全局 DBSCAN）

**用途：** 隔板近似水平、全箱行数在 Y 方向可一次分开时使用；同时也是每一竖带内部的聚类器。

**工序：**

1. 取每个多边形全部顶点的 Y，中心 Y = (Ymin + Ymax) / 2。
2. 确定邻域半径 `eps`：
   - 若未给比例，则 `eps = 1.2 × 框高中位数`（按框自身尺度自适应）。
   - 若给定 `eps_ratio`，则先用全部框的 Y 跨度当作“图像高度”的近似，`eps = eps_ratio × Y跨度`。
   - 分段投影里不用这套近似高度，而用真实图像高度计算 `eps_ratio`，避免 `image_path` 为空时打不开图。
3. 把各框中心 Y 看成一维点，做 DBSCAN。
4. 标签为 -1 的点视为噪声，不计入行数。
5. 各簇按中心 Y 从小到大重编号，保证 0 永远是最上行。

**实施要点：** `min_samples=1` 时，孤立点也可以成簇，末行只有少量岩芯时不容易被丢掉，但噪声也更容易变成“假行”。因此后面必须用分段投票和末行校验约束。

---

## 5. 指定行数分配（KMeans）

**用途：** 行数已经由人工指定，或已经由分段投票确定，只需要把每个框分到恰好 N 行。

**工序：**

1. 实际簇数 = min(指定行数, 框数量)。框比行数还少时，有多少框就按多少行处理。
2. 只有 1 行时，全部框归第 0 行，中心取全体 Y 均值，不再跑 KMeans。
3. 若提供了与行数等长的初始中心，则 `n_init=1`，用这组中心启动，避免随机初始化把弯曲隔板的行中心拉偏。
4. 聚类后仍按中心 Y 重排标签，输出顺序与“从上到下”一致。

分段投影的最终归属，全部走本工序，且全流程只允许调用一次。

---

## 6. 多边形竖向裁剪（Sutherland–Hodgman 的竖直线形式）

**用途：** 分段时不能把一个斜跨两段的岩芯整块算进某一段，否则该段的 Y 范围会被邻段“借高/借低”。

**规则：**

- 对竖直线 `x = x_boundary`，按“保留左侧”或“保留右侧”裁剪。
- 边的一端在内、一端在外时，用线性插值求交点，交点入新多边形。
- 一条竖带的有效区间为 `[x_start, x_end]`：先裁掉左边界以左，再裁掉右边界以右。
- 裁完后顶点少于 2 个，或裁后高度不足 1 像素，视为本段无效，丢弃。

**质量含义：** 段内用于聚类的 Y，是“这块岩芯落在本竖带里的那一段”的 Y，而不是整段岩芯中心点的 Y。隔板在 X 方向缓慢弯曲时，各竖带仍能看到相对水平的局部行结构。

---

## 7. 分段投影行检测（主算法）

### 7.1 分段参数

- 固定分成 **10** 条竖带，带宽 = 图像宽度 / 10。最后一段右边界略向外扩 1 像素，避免贴边顶点被裁掉。
- 框高取中位数 `median_height`，基准

  `eps_ratio_base = median_height / 图像高度 / 2`

  与 `caculate_eps_ratio`（平均框高 / 图高 / 2）同量级，但用中位数更抗破碎短框干扰。
- 每段 DBSCAN 实际使用 `eps_ratio = eps_ratio_base × 0.8`，`min_samples = 1`。
- 某段裁剪后有效多边形少于 2 个，或 DBSCAN 行数为 0，该段不参加投票。

### 7.2 行数投票

1. 收集各有效段的行数，按出现次数投票。
2. 票数最高者为众数 `voted_N`。
3. 多个行数并列最高票时，**取较小值**（偏保守，避免轻易多出一行）。

### 7.3 末行不满（N+1）候选规则

岩芯箱末行经常只在箱首（图像左侧）出现一小段，后面各段仍是 N 行。若只看众数，会把末行丢掉。

本交底采用“不保守包含”规则，只认第 0 段：

| 第 0 段是否检出众数+1 | 第 1 段是否检出 | 处理 |
|---|---|---|
| 是 | 否 | 暂定最终行数 = N+1（末行不满） |
| 是 | 是 | 同样暂定 N+1 |
| 否 | 是 | **不 +1**。末行不满必须从箱首段开始显现 |
| 否 | 否 | 维持众数 N |

第 2 段及以后即使也出现 N+1，视为可能误检，**不阻止**第 0 段触发的 +1，但后面这些段**不参与末行中心**的计算。

### 7.4 末行误判校验（仅在已暂定 +1 时执行）

目的：防止第 0 段把同一行的高低起伏、碎块或牌区噪声当成多出来的一行。

**数据一律用分段 DBSCAN 的裁剪多边形，不用 KMeans 结果。**

1. 第 0 段：取出标签 = `voted_N` 的裁剪多边形（即第 N+1 行），算中心 Y 均值，记为 `mean_Y_last`。
2. 第 1～4 段：只保留“行数恰好等于 N”的段作为参考段；在这些段里取标签 = `N-1` 的多边形（即第 N 行），汇总中心 Y 均值 `mean_Y_ref`。
3. 有效参考段至少 **2** 段，且两组都有多边形，才允许校验；否则数据不足，**不撤销**，维持 +1。
4. 差值比率 = `|mean_Y_last − mean_Y_ref| / 图像高度`。
5. 阈值 = `2 × (eps_ratio_base × 0.8)`。
6. 若差值比率 **小于** 阈值：两组其实在同一高度附近，判定第 0 段 N+1 为误检，最终行数改回 N。
7. 若差值比率 **大于等于** 阈值：多出来的行在 Y 上确实分开了，保留 N+1。

校验通过后，最终行数锁定，这之后才允许 KMeans。

### 7.5 初始簇中心汇总与一次 KMeans

按最终行数 `final_N` 准备长度为 N 的中心列表：

- 行数等于 `final_N` 的段：贡献该段全部簇中心。
- 若最终是 N+1，则第 2 段及以后即使也报了 N+1，**整段跳过**，其多报的末行中心不进入均值。
- 行数等于众数 N 的段：只贡献与 `final_N` 对齐的前缀中心（共同的上面几行）。
- 某行没有任何分段中心时，用已有中心的间距向外推一格；仍没有则填 0。

用这组均值作为 KMeans 初始中心，对**原始完整多边形**（不是裁剪片段）做一次指定行数聚类，得到最终 `row_labels` 和 `cluster_centers`。

---

## 8. 回次划分（遇牌截止、无牌延续）

行确定之后，才能划回次。回次是全箱连续编号，**不按行重置**。

### 8.1 牌的行归属

每个岩芯牌用其 Y（入口示例为点坐标的第二维）与各行岩芯中心 Y 的均值比距离，牌归入最近的一行。同行内的牌再按 X 从小到大排序。

### 8.2 回次数量

`回次数 = 全箱岩芯牌总数 + 1`。无牌时，全部岩芯归回次 0。

### 8.3 逐行扫描

维护全局计数器 `run_counter`，初始为 0。

对每一行，先按岩芯中心 X 从左到右排序，并用该行所有顶点的最小/最大 X 作为行的左右边界（不用中心点范围，避免边角岩芯落在边界外）。

- **本行无牌：** 本行全部岩芯写入当前 `run_counter`，计数器不变，下一行继续这个回次。
- **本行有 n 个牌：** 分割点 = 行左边界 + 各牌 X + 行右边界，并再排序一次（防止牌 X 落到岩芯范围之外造成区间乱序）。每个岩芯中心 X 落入哪个区间，回次号 = `run_counter + 区间序号`。最右一个区间用闭区间，其余为左闭右开。落空则归最近区间中点。本行结束后 `run_counter += n`。最后一个牌右侧的回次会延续到下一行。

### 8.4 扫尾

若仍有岩芯标签为 -1，按其中心 X 到各回次左边界的距离，归入最近回次。

**现场理解：** 牌是回次的结束标记，不是开始标记。牌左边是即将结束的回次，牌右边是新回次；下一行若暂时无牌，新回次可以一直往下走。

---

## 9. 可视化与交付文件

| 产物 | 触发条件 | 内容 |
|---|---|---|
| `1.jpg` | 调用全局行聚类可视化 | 左图：框按行着色；右图：中心 Y 直方图及行中心线 |
| `2.jpg` | 调用回次可视化 | 底图上按回次着色；有效 RQD 岩芯黄色加粗（需传入 RQD 结果才高亮） |
| `{图名}_segment_debug.jpg` | 分段投影且提供了透视校图 | 写到会话目录下 `4_table_info_image/`。含各段行数、投票、是否 +1/是否撤销、KMeans 行中心、每段裁剪多边形按 DBSCAN 簇着色 |
| `{图名}_plus1_revoke.jpg` | 走到末行误判校验且数据足够 | 同目录。蓝：第 0 段待校验末行；绿：参考段第 N 行；红/绿线：两组平均 Y 及判定结论 |

调试图中文字体优先找 Windows 黑体/雅黑/宋体。Linux 无这些字体时回退默认字体，中文标题可能缺字，**不影响数值结果**。

可视化失败必须吞掉异常并继续返回行数，不得因为画图失败让主流程中断。

---

## 10. 关键参数一览

| 参数 | 默认/取值 | 作用 | 调整原则 |
|---|---|---|---|
| 竖带数量 | 10 | 局部化弯曲隔板 | 过少则弯曲仍被抹平；过多则每段样本不足 |
| `eps_ratio_base` | 中位框高 / 图高 / 2 | 控制“多近算同一行” | 框高低估会拆行，高估会并行 |
| 分段 DBSCAN 系数 | ×0.8 | 段内比全局略紧 | 破碎多时可再观察是否假行 |
| `min_samples` | 1 | 允许末行稀疏散点成行 | 噪声大的箱子更依赖投票和校验，不要单独放宽 |
| 众数并列 | 取较小行数 | 防止轻易多行 | 与“第 0 段 N+1 再放宽”配套 |
| N+1 触发段 | 仅第 0 段 | 末行不满从箱首开始 | 第 1 段单独 N+1 不采纳 |
| 参考段 | 第 1～4 段中恰好 N 行者 | 校验末行是否真的分开 | 不足 2 段则不撤销 |
| 撤销阈值 | 2 × 段内 eps_ratio | Y 差相对图高过小则视为误检 | 阈值过小会留下假行，过大可能把真末行撤掉 |
| KMeans `random_state` | 42 | 复现 | 有自定义中心时 `n_init=1` |
| 回次 X 容差比例 | 0.1 | 接口保留，当前扫描逻辑未使用 | 不要按该参数理解现网行为 |

函数名 `caculate_eps_ratio` 为历史拼写，交底与代码保持一致，实施时勿自行改名导致调用失败。

---

## 11. 入口脚本当前行为（与主推荐方案的差异）

`__main__` 现行做法：

1. 用 `runCoreModel_OutputCoreBoxCoor` 检测指定路径图片。
2. 用 `caculate_eps_ratio` 得到比例后，走 **全局 DBSCAN**，不是分段投影。
3. 岩芯牌写死为三个点 `[1329,945]`、`[2856,1339]`、`[3239,2250]`，再扩成宽 50、高 100 的矩形用于画图。
4. 图片路径写死为开发机 Windows 路径，且依赖同目录（或包内）的 `ModelRun`。

**实施要求：** 生产接入应改为调用 `detect_rows_by_segmented_projection`，并传入真实图像宽高与透视校正图路径；岩芯牌必须来自牌检测，不得使用示例坐标。

---

## 12. 运行条件与已知限制

1. **模型依赖：** 本文件不能单独作为检测程序。缺少 `ModelRun.py` 或检测权重时，入口必然失败。行聚类子程序在给定 `boxes` 后可独立运行。
2. **第三方库：** `numpy`、`scikit-learn`（DBSCAN/KMeans）、`matplotlib`、`Pillow`、`opencv`（仅入口读图）。无界面环境需使用非交互后端，`plt.show()` 只会告警，保存文件仍有效。
3. **路径假设：** 调试图默认“透视校正图的上一级目录”为会话根，其下创建 `4_table_info_image`。目录结构不一致时，图会写到错误位置，但不影响行数。
4. **牌框格式不一致：** 回次扫描按 `[x, y]` 中心点使用牌；回次画图按 `[x1,y1,x2,y2]`。接入时必须按阶段转换，不能把两种格式直接混传。
5. **RQD 高亮：** 回次图的黄色有效岩芯依赖外部 `rqd_results`。不传入时只画回次颜色，不代表没有有效岩芯。
6. **中文显示：** Linux/无 Windows 字体环境下，调试图中文可能变成方框，以日志中的数字结论为准。

---

## 13. 复核与验收要点

现场或测试验收时，按下列顺序核对，不要只看最终行数一个数字：

1. 各竖带有效多边形数量是否合理；箱体左右留白段允许跳过。
2. 各段行数列表与投票分布是否与肉眼排数一致。
3. 若启用 N+1：第 0 段是否确实多出一行；校验图上蓝、绿两组平均 Y 是否明显分开。
4. 最终 KMeans 红线是否落在各排岩芯中部，而不是落在隔板上。
5. 回次：每张牌是否成为某一回次的右边界；无牌行是否延续上一回次而不是重新从 0 编号。
6. 主流程日志中应出现“KMeans（仅一次）”；若出现两次不同 N 的聚类，视为实现偏离本交底。

---

## 14. 交底结论

本模块的技术核心是：**先在 10 条竖带上用裁剪多边形做局部 DBSCAN，用投票确定行数，用第 0 段处理末行不满，用第 1～4 段做误检撤销，最后只做一次带初始中心的 KMeans 把整框归行；行确定后再按岩芯牌从左到右、通箱递增划分回次。**

算法数值部分在给定多边形框后可以独立跑通。整图端到端能否运行，取决于检测模型、透视校正图、岩芯牌检测和目录约定是否齐全，不在本文件内部闭合。
