# 岩芯箱行检测与回次划分方案  
## 背景技术与要解决的技术问题

---

## 一、背景技术

### 1.1 所属领域

本方案属于地质钻探岩芯数字化编录领域，具体涉及岩芯箱图像中岩芯实例的**空间结构解析**：在目标检测已经给出各段岩芯轮廓之后，自动判定箱内岩芯行数、各段岩芯的行归属，并结合岩芯牌划分回次，为后续回次长度统计、RQD 计算和编录表生成提供结构化输入。

岩芯自钻孔取出后，按现场规程依次放入岩芯箱。箱内通常以隔板分成若干水平行；同一回次的岩芯按钻孔深度连续摆放，回次结束处放置岩芯牌。数字编录系统用相机拍摄整箱照片，经透视校正后，由检测模型输出每一段岩芯的多边形框，以及岩芯牌的位置。检测只回答“哪里有一段岩芯”，并不回答“这是第几行、属于第几回次”。没有行与回次，检测结果无法进入编录表，也无法按回次汇总 RQD。

### 1.2 现有技术的一般流程

现有数字化岩芯箱系统通常分为两段：

**前段：成像与实例检测。** 对岩芯箱照片做透视校正，再用检测或分割模型得到岩芯实例。输出多为轴对齐矩形，或带四个以上顶点的多边形。这一段解决的是“框出岩芯”，本方案默认其已经完成。

**后段：结构解析。** 在检测框集合上恢复箱内几何秩序，至少包括：

1. 判定行数，并把每一检测框标到从上到下的某一行；
2. 在行内按左右顺序排列岩芯，遇到岩芯牌则结束当前回次、开启下一回次；
3. 将像素长度换算为实际长度，按回次统计 RQD。

本方案针对的是后段中的第 1、2 步。第 3 步依赖行、回次已经正确，本身不是本方案的任务。

### 1.3 现有行划分方法

在已有检测框的前提下，常见行划分做法如下。

**（1）人工指定或按箱型写死行数。**  
由编录员目视填行数，或按“某类箱子固定 4 行、5 行”配置。行数已知后再按 Y 坐标排序或做 KMeans。该方法把最容易错的“行数”交给人，无法在批量照片上自动闭环；箱型一变或末行不满，写死行数即失效。

**（2）Y 方向投影直方图数峰。**  
把各检测框的中心 Y（或框覆盖的 Y 区间）投影成一维直方图，用峰的个数当行数，用峰的位置当行中心。隔板平直、行距均匀、曝光稳定时可用。隔板弯曲或残余透视使同一行的 Y 随 X 漂移后，单峰被拉宽、邻峰粘连，峰数少于真实行数；破碎岩芯、岩屑、牌区则可能多出假峰。

**（3）对全部框的中心 Y 做一次全局聚类。**  
将每个岩芯框缩成一个 Y 值，用 DBSCAN、层次聚类或高斯混合在一维 Y 上聚类。DBSCAN 不必预先给定行数，KMeans 必须先给定行数。其共同前提是：**同一行的中心 Y 在全图范围内彼此接近，不同行的中心 Y 彼此分开，行间距大于行内 Y 离散。**  
现场岩芯箱常不满足这一前提。隔板加工与放置难以保证全程水平，照片即使做了透视校正仍会残留行向弯曲。同一物理行左侧与右侧的中心 Y 差，可以接近甚至超过相邻两行的标称间距。此时全局一维聚类会把邻行并成一行，或把一行拆成多行。行数一旦错，后续回次切分和 RQD 全部错位。

**（4）按图像高度等分，或按隔板检测线切割。**  
等分假定各行等高且铺满画面，与实际留白、末行不满、隔板间距不等均冲突。单独再检隔板，等于在岩芯检测之外再维护一套线检测，隔板被岩芯遮挡、反光或缺失时不稳定，工程上往往希望只依赖已有岩芯多边形。

**（5）沿图像宽度划分竖条，按框中心点把整段岩芯丢进某一条再分别聚类。**  
这是对“全局 Y 受弯曲影响”的直观改进：希望在窄带内隔板近似水平，带内再做 Y 聚类。若分配规则是“中心 X 落在哪条带，整框就属于哪条带”，则斜跨、弯折的多边形会被整段计入中心所在带，该带的 Y 统计混入邻带高度，局部行结构再次被污染。竖条结果如何合成全图行数，现有做法也缺少稳定规则：有的取各条行数的最大值，破碎段一多就虚高；有的取众数，末行只出现在箱首少数条带时就被投票丢掉。

### 1.4 现有回次划分方法

行确定之后，回次划分的现有做法主要是：

- 每一行单独从左到右编号，行与行之间的回次号不连续；
- 或依赖岩芯牌 OCR 读出回次号，OCR 失败则整段无法衔接；
- 或假定每行恰好一个回次、或牌总在行末。

现场规则通常是：回次沿整箱连续递增，**岩芯牌表示当前回次结束而不是开始**；某一行可以没有牌（整行仍属上一回次），也可以有多块牌（一行内切出多个回次）；上一行最后一个牌之后的回次，要延续到下一行行首。现有“按行重置编号”或“一行一回次”与该规程不一致。更关键的是，回次扫描以行为坐标系：行数或行归属一旦错误，牌会被贴到错误的行上，回次在错误的位置切断。

### 1.5 现有技术的综合缺陷

归纳起来，现有技术在“检测已经完成”之后，仍存在下列相互纠缠的不足：

1. **行中心随列位置变化时，全局一维 Y 方法失效。** 弯曲隔板、残余透视使同一行的 Y 区间与邻行重叠，直方图和全局 DBSCAN/KMeans 都会并行走或拆行。  
2. **末行不满与局部噪声，在统计上表现相同，都是“少数位置多出一行”。** 取众数则丢掉真实末行；取最大值则把破碎、掉块、牌区起伏当成新行。  
3. **用框的中心点代替框在某条竖带内的真实截面，** 使分段统计不能代表该带内的岩芯几何。  
4. **行数与行归属耦合。** KMeans 必须先有 K。若先在错误行数下聚类，再根据结果修正行数，簇中心和归属会被第一次错误的 K 带偏；现场又需要自动定 K，不能依赖人工每次指定。  
5. **检测框是多边形，不是理想水平条。** 以轴对齐外接矩形中心代替多边形真实高度范围，弯曲、斜切岩芯的 Y 中心会偏离实体。  
6. **回次必须跨行连续、以牌为结束标记。** 现有按行独立编号的方法不能表达“无牌则延续”；而回次正确性严格依赖行结构正确，行错误会放大成整箱编录错误。

上述缺陷在隔板平直、满箱、无破碎的实验室照片上往往不出现，因而容易被全局 Y 聚类的简单实现掩盖。生产照片上弯曲、末行不满、局部稀疏经常同时出现，现有单一手段无法同时稳住行数和行归属。

---

## 二、要解决的技术问题

本方案要解决的技术问题是：

**在岩芯箱图像已经得到各段岩芯的多边形检测结果（以及岩芯牌位置）的前提下，如何在隔板弯曲或存在透视残余、末行岩芯不满箱、局部破碎稀疏等条件同时存在时，自动、稳定地确定箱内岩芯行数，将每一段岩芯归属到从上到下的正确行，并按现场规程完成跨行连续的回次划分，避免现有全局一维 Y 聚类的并行走/拆行，以及避免简单统计规则漏计真实末行或把局部噪声升格为新行。**

该总问题可分解为下列必须同时成立的具体问题。

### 2.1 弯曲条件下如何仍能得到正确行数

隔板沿箱长方向弯曲时，同一物理行的中心 Y 随 X 连续变化，变化幅度可以与行距同一量级。此时不能再把整图上的 Y 当作可分的一维点集。需要一种方法：在局部列范围内使隔板近似平直从而可数出行数，再把各局部结果合成全箱行数，使合成结果对单段噪声不敏感，对整箱一致弯曲保持稳定。

### 2.2 如何把“箱首真实不满末行”从“局部假行”中分开

末行岩芯常常只占据箱首一侧，其余列仍为 N 行。若以各局部行数的众数为准，真实的第 N+1 行会被当成少数派丢掉；若以各局部行数的最大值为准，某一列里因破碎、检测抖动多出来的假行又会被抬成全箱行数。需要一套可执行的判定规则：在何种局部证据下允许把行数从众数 N 增为 N+1，以及在何种几何证据下应当撤销这次增行，从而在“漏末行”和“假增行”之间取得可复核的折中。

### 2.3 分段时如何使用岩芯在该竖带内的真实几何，而不是框中心

岩芯多边形经常斜跨两条甚至多条竖带。若整框只按其质心或外接矩形中心划入一条带，该带的 Y 范围会被邻带高度污染，局部聚类再次失真。需要在分段时按竖带边界裁剪多边形，使每一条带只用“落在本带内的那一段轮廓”计算高度和中心，以保证局部行结构反映的是该列的截面，而不是检测框的中心点落点。

### 2.4 如何先独立确定行数，再做一次且仅一次的行归属

行归属若采用 KMeans 一类必须预先指定簇数的方法，则行数估计与样本分配不能缠在同一次迭代里。需要避免“先按 N+1 聚完，发现错了再按 N 重聚”：第一次错误的簇中心会污染判定，也会让结果不可复现。正确的顺序应是：行数在局部聚类的原始结果上先行锁定，然后对全部原始多边形做唯一一次指定行数的分配，并且分配所用的初始中心应来自已经通过校验的局部簇中心，而不是随机初始化。

### 2.5 行标签如何与空间顺序一致

聚类算法给出的簇编号是任意的。编录要求第 1 行永远是图像上方（Y 较小）的一行，其余行严格按从上到下递增。无论局部 DBSCAN 还是最终 KMeans，都需要把簇按中心 Y 重排后再对外输出，否则回次扫描会按错误的行序进行。

### 2.6 回次如何按“遇牌结束、无牌延续”在全箱连续编号

在行结构确定之后，还需要按现场摆放规程划分回次：全箱使用同一个回次计数器，不在行首清零；岩芯牌是当前回次的结束标记；本行无牌则整行属于当前回次并延续到下一行；本行有多块牌则在行内按牌的 X 位置切开，最后一个牌右侧的回次继续进入下一行。该规则必须以正确的行归属为前提，且牌本身也要先按 Y 贴到最近的一行。现有按行独立编号或依赖 OCR 读号的方法不能替代这一几何扫描问题。

### 2.7 判定过程如何可复核

行数从众数改为 N+1、或撤销 N+1，都属于对自动结果的关键决策。现场验收不能只看一个最终整数。需要把各竖带行数、投票分布、末行校验所用的两组平均 Y、最终行中心等过程量固化为调试图和日志，使错因可定位到“某一段多检”“校验阈值未过”或“最终分配中心偏移”，而不是整箱结果无法解释。

---

## 三、问题边界（不在本方案内解决）

下列问题构成本方案的输入条件或后续工序，不作为本方案要解决的技术问题：

- 岩芯与岩芯牌的检测、分割及模型训练；
- 照片的透视校正、镜头畸变校正；
- 像素到毫米的比例尺标定，以及 RQD 公式中的长度阈值选取；
- 岩芯牌文字的 OCR 识别（本方案只使用牌的几何位置作为回次结束标记）。

本方案的输入是透视校正后的图像尺寸、岩芯多边形集合和岩芯牌位置；输出是行数、各框行号、行中心 Y，以及回次号与回次区间。背景技术中所述缺陷，均发生在这一输入输出之间。
