# 一种融合合成孔径雷达与光学遥感的地块级归一化植被指数云缺失填补方法及系统

**专利交底书**

| 项目 | 内容 |
|---|---|
| 发明名称 | 一种融合合成孔径雷达与光学遥感的地块级归一化植被指数云缺失填补方法及系统 |
| 建议专利类型 | 发明专利 |
| 交底对象 | 专利代理师 / 专利工程师 |
| 交底范围 | 数据立方构建、模拟云监督、时空变换器重建、通道级特征门控、蒙特卡洛弃权不确定性估计及预测区间校准 |
| 不在本次交底范围 | 地块矢量的自动勾绘、云检测网络本身、业务化地图发布与前端展示；训练超参的具体取值不构成保护核心 |
| 技术来源 | 草地地块 Patch 级 NDVI 云填补实验系统（B1–B4 消融架构） |

---

## 一、技术领域

本方案属于定量遥感与农业遥感信息技术领域，具体涉及多云多雾条件下农田/草地**地块级归一化植被指数（NDVI）时序重建**。更具体地，涉及：

1. 以地块为采样单元，构建「时间 × 空间邻域 × 多源特征」的小尺度数据立方；
2. 在光学通道缺失、微波通道仍可用的条件下，用时空变换器填补被云污染的 NDVI；
3. 对雷达与光学特征进行可学习的通道门控（或离线重要性筛选），抑制冗余特征；
4. 对填补结果给出经校准的预测区间，而不仅给出点估计。

本方案的典型应用对象为温带草地或农田地块在生长季内的中高分辨率卫星观测（实施例以 Sentinel-1 / Sentinel-2、10 m、6 日合成、5×5 像元邻域为例），但不限于上述传感器与格网尺度。

---

## 二、背景技术

### 2.1 应用需求

NDVI 是植被长势、物候和草地生产力监测的基础参数。Landsat、Sentinel-2 一类 10–30 m 光学数据空间细节足够，但重访周期有限，且云、云影、雾会造成大量缺失。在多云山区、沿海和春夏生长季，一个完整生长季内可用晴空观测往往不足，业务产品出现「空洞」或时间不连续。微波合成孔径雷达（SAR）可穿云，后向散射与植被水分、结构相关，常被用作光学缺失时段的辅助信息。工程上需要一种能够：

- 在地块尺度保留空间异质性；
- 同时利用 SAR 与残存晴空 NDVI；
- 对特征冗余和预测可靠性有所约束

的云缺失填补方法。

### 2.2 现有技术的一般流程

现有 NDVI 时序重建大致遵循：光学数据预处理与云掩膜 →（可选）辅助数据配准 → 缺失值估计 → 后处理平滑。按技术路线，常见方法可分为以下几类。

### 2.3 现有方法及其现场失效原因

**（1）单点时序滤波与谐波拟合。**  
对每个像元独立做 Savitzky–Golay、谐波分析、Whittaker 或线性/样条插值。前提是同一像元在缺失时段前后仍有足够晴空点，且物候变化平滑。持续云盖、生长季峰值被整段挡住时，滤波会把邻域晴空值「抹平」或外推失真；该方法也不使用 SAR，无法在光学全缺时提供新信息。

**（2）多源时空融合（以中低分辨率光学产品或相邻传感器为参考）。**  
如 STARFM 类方法、以 MODIS/GLASS 为参考的形态匹配与线性校正。前提是粗分辨率产品与 Landsat/Sentinel-2 在物候形态上可配准。地块内部 10 m 级异质性（沟渠、斑块、田埂）会被粗分辨率参考抹掉；且仍高度依赖光学参考，持续云区参考本身不可靠。

**（3）SAR–光学经验回归或机器学习填补。**  
在晴空样本上建立 VV/VH 等到 NDVI 的回归、随机森林或浅层网络，再应用到有云像元。前提是 SAR–NDVI 关系在时空上稳定。实际中该关系随物候、土壤湿度、入射角和轨道方向变化；把 VV、VH、差值、比值等一并输入时，特征共线严重，离线筛选与重建网络割裂，容易过拟合或把有用通道剔错。

**（4）像元级深度时序网络（LSTM、BiLSTM、一维 Transformer）。**  
把单像元多时相特征送入循环或注意力网络，用晴空 NDVI 作监督。能够刻画物候前后依赖，但**丢弃了地块内邻域结构**：同一地块相邻像元共享物候和土壤背景，云往往成片出现，邻域晴空像元是重要证据。纯像元模型无法利用这一空间冗余。

**（5）直接在全图上做大窗口卷积/视觉 Transformer。**  
能够利用空间信息，但计算图以整景或大瓦片为单位，难以与**矢量地块**的业务单元对齐；地块边界内外的光谱差异会被卷积抹平；也缺少针对「仅在原本晴空位置构造监督、禁止用云污染 NDVI 当标签」的训练协议。

**（6）不确定性方面。**  
多数填补方法只输出 NDVI 点值。少数深度学习工作给出 Dropout 或集成方差，但未将预测区间的经验覆盖率（PICP）校准到名义置信水平，区间过窄或过宽，无法支撑「带风险的农情决策」。

### 2.4 现有技术的综合缺陷

综合上述路线，现有技术至少同时存在以下缺陷：

1. **采样单元与业务单元不一致**：像元独立处理，或整景大窗处理，难以在「一块草地/一块农田」上同时利用局地空间邻域与生长季时序。
2. **监督信号被云污染**：用含云像元的 NDVI 当回归目标，或在全时序上无差别求损失，模型学习的是噪声而非植被动态。
3. **多源特征简单堆叠**：SAR 多极化及其派生量与 NDVI 一并输入，缺少与重建网络联合的通道选择/门控，冗余和共线无法在训练中被抑制。
4. **离线特征选择与重建割裂**：先按相关系数或随机森林重要性剔特征，再训练填补网络，选择准则与最终重建误差不一致。
5. **只给点估计**：缺少与填补模型配套的、可校准的预测区间，业务上无法区分「高把握填补」与「不可信填补」。
6. **空间网格无序时，所谓「Patch 模型」名不副实**：若从地球引擎等平台按无序点集抽样再强行 reshape 成二维块，邻域几何被破坏，空间注意力失去物理含义。

---

## 三、要解决的技术问题

**在已有地块矢量（或地块中心点）以及光学、SAR 时间序列的前提下，如何在云成片出现、晴空观测稀疏、SAR–光学关系随物候变化、多通道特征冗余等条件同时存在时，自动、稳定地重建地块邻域内各时相的 NDVI，并给出经过覆盖率校准的预测区间，避免现有单点插值抹平物候、整景模型跨出地块、含云标签污染监督、特征堆叠过拟合以及无校准不确定性等问题。**

需要同时满足的分问题：

1. **地块级数据立方如何建**：以地块为中心取固定邻域，时间上规则合成，空间上按地理行列对齐，光学给出 NDVI 与晴空掩膜，SAR 给出可穿云特征。
2. **监督如何构造**：只在「天然晴空」像元上按比例人为遮挡光学通道，损失只在这些被遮挡的真值点上计算。
3. **模型如何同时用时序和邻域**：把「时相 × 邻域像元」作为令牌，用时空位置编码与变换器完成缺失 NDVI 回归。
4. **特征如何选**：既能离线按重要性保留 Top-k，也能用可微通道门控与重建网络联合训练；光学通道可赋予先验初值。
5. **可靠性如何给**：同一套带弃权的网络，推理时多次随机前向得到均值与标准差，再在校准集上搜索尺度因子，使经验覆盖率逼近名义置信水平。

### 问题边界（不在本方案内解决）

- 地块边界本身的遥感提取或权属划分；
- 云/云影检测器的训练（实施例直接使用 Sentinel-2 SCL 等级）；
- 非植被参数（LAI、生物量、土壤水分）的反演；
- 全球无地块矢量条件下的纯影像瓦片生产（可作为扩展，不是本交底的必要技术特征）。

---

## 四、发明内容（技术方案）

### 4.1 技术核心（定稿原则）

本方案的技术核心可以概括为四条必须同时成立的原则：

1. **业务单元是地块邻域，不是整景，也不是孤立像元。** 每个样本是「中心点周围 \(H\times W\) 的规则格网 × \(T\) 个合成时段 × \(C\) 个特征」。
2. **标签必须是晴空真值。** 训练时只把天然晴空位置上的 NDVI 挖掉当作待填目标；SAR 及未被挖掉的 NDVI 仍保留。禁止用云区光学值当监督。
3. **特征选择与重建可以是一条计算图。** 通道门控（或离线 Top-k 作为对照/降级路径）作用在特征维上，再进入时空变换器；门控参数与变换器参数一起反传。
4. **点估计与区间估计共用同一套网络。** 用训练期弃权、推理期蒙特卡洛采样得到均值和标准差，再用与训练分离的校准样本把标准差缩放到目标覆盖率。空间归属、损失位置、校准位置必须使用同一套晴空掩膜定义，禁止三套掩膜各算各的。

### 4.2 总体工艺流程

```
地块矢量 / 中心点
        │
        ▼
① 规则时空立方构建
   · 时间：生长季按 Δt 日合成，共 T 步
   · 空间：中心点外扩，H×W 像元，按地理行列对齐（禁止无序点 reshape）
   · 光学：NDVI + 晴空掩膜 M_nat
   · SAR：VV、VH 及派生量（差、比等）
        │
        ▼
② 模拟云监督构造
   · 仅在 M_nat=1 的位置，按比例 r 抽样生成评价掩膜 M_eval
   · 将对应位置的 NDVI 通道改写为缺失标记 τ
   · 其他特征保持原值
        │
        ▼
③ 通道处理（二选一或级联）
   · 路径 A（离线）：按特征与晴空 NDVI 的重要性排序，保留 Top-k
   · 路径 B（主路径）：可微 sigmoid 门控 g_c = σ(ℓ_c / τ_temp)，X ← X ⊙ g
        │
        ▼
④ 时空变换器重建
   · 每个（t,i,j）为一个令牌，线性嵌入
   · 时间位置编码 ⊕ 空间位置编码
   · 多层 Pre-Norm Transformer
   · 令牌级回归头，输出 NDVI 估计 Ŷ
        │
        ▼
⑤ 掩膜损失训练
   · L = mean( (Ŷ − Y)^2 ⊙ M_eval )
   · 早停与学习率衰减以验证集掩膜损失为准
        │
        ▼
⑥ 推理与不确定性
   · 保持弃权开启，重复前向 N 次
   · 均值 μ、标准差 σ
   · 校准集上二分搜索尺度 s，使 PICP(μ ± z·s·σ) ≈ 名义置信度
        │
        ▼
⑦ 输出
   · 填补后的地块 NDVI 立方
   · 校准后的预测区间 [L,U]
   ·（可选）通道门控权重，用于解释特征贡献
```

### 4.3 步骤①：地块级多源时空立方构建

**用途：** 把矢量地块变成网络可消费的、空间有序的张量。

**工序：**

1. 读取地块几何，取其质心；若几何为投影坐标（实施例为 EPSG:3346），先变换到地理坐标。
2. 以质心为中心，按地面分辨率 \(s\)（实施例 10 m）和邻域边长 \(H=W=P\)（实施例 \(P=5\)，对应 50 m×50 m）计算经纬度半宽，生成矩形窗口。
3. 将生长季 \([D_0, D_1]\) 切成 \(T\) 个长度为 \(\Delta t\) 的时段（实施例：4 月 9 日至 9 月 25 日，\(\Delta t=6\) 日，\(T=29\)）。
4. 每个时段内：
   - SAR：筛选 IW、含 VV 极化的 Sentinel-1 GRD，对 VV、VH 取中值合成；
   - 光学：Sentinel-2 SR 计算 NDVI \(=(B8-B4)/(B8+B4)\)；以场景分类层 SCL∈{4,5}（植被、裸土）定义为该观测「清洁」；时段 NDVI 取中值，清洁标记取时段内逻辑或（任一清洁即记为该格点该时段可用）。
5. 派生特征（实施例）：  
   - 交叉差 \(CROSS = VV - VH\)（分贝域差，表征极化对比）；  
   - 比值 \(RATIO = VV / VH\)（分母为 0 则记为缺失）。
6. **空间对齐（必要技术特征）：** 采样必须携带像元中心坐标，按窗口内行、列索引填入 \(H\times W\) 网格；点数不足则在网格边缘以缺失填充，禁止把无序采样点按数组顺序 reshape 成块。否则后续空间位置编码与邻域注意力没有地理含义。
7. 得到样本：

| 符号 | 形状 | 含义 |
|---|---|---|
| \(X\) | \(T\times H\times W\times C\) | 多源特征，实施例 \(C=5\)：VV、VH、CROSS、RATIO、NDVI |
| \(Y\) | \(T\times H\times W\) | 光学 NDVI 真值（含天然缺失） |
| \(M_{nat}\) | \(T\times H\times W\) | 天然晴空掩膜，1=该格点该时段存在清洁光学观测 |

光学 NDVI 的天然缺失位置写入缺失标记 \(\tau\)（实施例 \(\tau=-100\)，远离 NDVI 物理值域，便于网络识别「无观测」）；SAR 缺失在实施例中填 0，作为可替换实现，优选方案为另设 SAR 有效掩膜，避免与真实 0 dB 混淆。

### 4.4 步骤②：仅在晴空真值上构造模拟云监督

**用途：** 让网络学习「看见 SAR 和残存 NDVI → 恢复被挡住的真值」，而不是拟合云污染值。

**工序：**

1. 对每个时相 \(t\)，在 \(M_{nat}(t)=1\) 的格点集合中，以比例 \(r\)（实施例 \(r=0.3\)）独立伯努利抽样，得到评价掩膜 \(M_{eval}\)。若该时相无任何晴空格点，则该时相不产生监督。
2. 输入立方中，仅将 NDVI 通道在 \(M_{eval}=1\) 处改写为 \(\tau\)；SAR 通道与其余位置的 NDVI **保持不变**。
3. 训练集可将同一地块重复构造 \(A\) 套不同掩膜（实施例 \(A=10\)）作为离线增强；验证/测试使用固定掩膜、增强倍数为 1。
4. 样本划分以**地块**为单元（按地块标识或所属区域分块），禁止把同一地块的不同掩膜版本分到训练与测试；推荐空间分区或跨年（如一年建模、次年测试）作为泛化评价。

**定稿原则：** 损失函数的统计范围、精度指标的统计范围、不确定性校准的统计范围，三者必须都是 \(M_{eval}=1\) 的位置。\(M_{eval}\) 必须被 \(M_{nat}\) 包含。

### 4.5 步骤③：通道级特征处理

本步骤提供两条可单独主张、也可作为同一发明不同实施例的路径。

#### 4.5.1 路径 A：离线重要性筛选（对照/降级路径）

在训练集、且 \(M_{nat}=1\) 的样本上，对每一特征通道 \(c\) 计算其与 NDVI 真值的绝对相关系数（或其他单变量/多变量重要性）。按重要性降序取前 \(k\) 个通道（实施例 \(k=3\)），将输入从 \(C\) 维切到 \(k\) 维后再送入变换器。

该路径训练与选择分两阶段，实现简单，可作为计算资源受限时的降级方案，以及用于消融证明「联合门控」的必要性。

#### 4.5.2 路径 B：可微通道门控（主路径）

设可学习对数几率向量 \(\boldsymbol{\ell}\in\mathbb{R}^{C}\)，温度 \(\tau_{temp}>0\)（实施例初值 1.0，可在训练中退火，非必需）。门控

\[
g_c = \sigma(\ell_c / \tau_{temp}),\qquad
\tilde{X}_{\ldots,c} = X_{\ldots,c}\cdot g_c
\]

即对整条特征维做广播乘法，不改变张量形状。光学 NDVI 通道对应的 \(\ell_{NDVI}\) 可赋予正的先验初值（实施例 \(+2.0\)），使训练初期更信任光学观测，再由数据决定是否下调。

**说明：** 门控是软选择。若权利要求需要「恰好保留 k 个通道」，可采用以下任一加严手段（均为本方案的等价实施例）：

- 对 \(g\) 施加 L1 / 熵惩罚，使多数门趋近 0；
- 用 Gumbel-Softmax 或可微 Top-k 掩膜；
- 训练后再按 \(g_c\) 排序做硬裁剪并微调。

路径 B 与变换器共享反向传播，选择准则直接服务重建误差，这是相对路径 A 的区别技术特征。

### 4.6 步骤④：时空令牌化变换器

**用途：** 在地块邻域内同时建模时间依赖与空间邻域依赖，输出每个格点每个时相的 NDVI。

**工序：**

1. 将 \(\tilde{X}\) 的每个 \((t,i,j)\) 视为一个令牌，令牌数 \(N_{tok}=T\cdot H\cdot W\)（实施例 \(29\times5\times5=725\)）。
2. 线性嵌入 \(\mathbb{R}^{C}\rightarrow\mathbb{R}^{d}\)（实施例 \(d=64\)）。
3. **时空位置编码：** 时间维正弦编码与空间维正弦编码各占 \(d/2\) 维后拼接。空间编码优选二维（行、列分别编码）再拼接；一维光栅序编码为简化实施例，精度通常弱于二维。
4. Pre-Norm Transformer 编码器（实施例：2 层、4 头、前馈 128、GELU、dropout=0.1）。
5. 令牌级 MLP 回归头，输出标量；再经值域约束（实施例为 sigmoid，将输出限制在 \([0,1]\)）。若目标区存在显著负 NDVI，值域约束可改为 \(\tanh\) 或线性头加截断，作为等价实施例。

前向可写为：

\[
\hat{Y} = f_{\theta}\big(G_{\boldsymbol{\ell}}(X)\big)\in[0,1]^{T\times H\times W}
\]

其中 \(G_{\boldsymbol{\ell}}\) 为步骤③的门控（路径 A 则为硬切片）。

### 4.7 步骤⑤：掩膜均方误差与训练规程

损失仅在模拟云位置上计算：

\[
\mathcal{L}
= \frac{\sum_{t,i,j} M_{eval}(t,i,j)\,(\hat{Y}(t,i,j)-Y(t,i,j))^2}
{\sum_{t,i,j} M_{eval}(t,i,j)}
\]

优化器实施例为 Adam，初始学习率 0.005，梯度范数裁剪 1.0；验证损失平台后以 0.5 倍衰减，连续若干轮不降则早停，回载验证集最优权重。

评价指标在同一 \(M_{eval}\) 上计算平均绝对误差（MAE）和决定系数 \(R^{2}\)。**\(R^{2}\) 必须以真值为第一自变量、预测为第二自变量**，顺序不可颠倒。

消融结构（对应实验 B1–B4，用于说明各技术特征的可分离性，不是使用上的必选步骤）：

| 结构 | 特征处理 | 推理 |
|---|---|---|
| B1 | 全通道，无门控 | 单次前向点估计 |
| B2 | 路径 A，Top-k | 单次前向点估计 |
| B3 | 路径 B，可微门控 | 单次前向点估计 |
| B4 | 路径 B | 蒙特卡洛弃权 + 区间校准 |

### 4.8 步骤⑥：蒙特卡洛弃权与预测区间校准

**用途：** 为每个填补值提供「带置信水平的区间」，并纠正原始方差系统性偏小或偏大。

**工序：**

1. 变换器层间弃权在训练期开启。推理不确定性时**保持训练模式**（弃权不关闭），对同一输入重复 \(N_{mc}\) 次前向（实施例 50 次），得样本 \(\{\hat{Y}^{(n)}\}\)。
2. 逐位置

\[
\mu = \mathrm{mean}_n\hat{Y}^{(n)},\qquad
\sigma = \mathrm{std}_n\hat{Y}^{(n)}
\]

3. 名义置信度 \(\alpha\)（如 0.80、0.90、0.95、0.99）对应标准正态分位 \(z_{\alpha}\)。未校准区间为 \([\mu-z_{\alpha}\sigma,\ \mu+z_{\alpha}\sigma]\)。
4. **尺度校准：** 在与测试地块分离的校准样本上，对尺度 \(s\) 在 \([s_{min},s_{max}]\)（实施例 \([0.1,20]\)）做二分搜索，使经验覆盖率

\[
\mathrm{PICP}(s)
= \frac{\sum M_{eval}\cdot\mathbf{1}\{Y\in[\mu-z_{\alpha}s\sigma,\ \mu+z_{\alpha}s\sigma]\}}
{\sum M_{eval}}
\]

逼近 \(\alpha\)。将求得的 \(s_{\alpha}\) 固定，用于测试或业务推理。同时报告区间平均宽度 MPIW，用于在覆盖率达标的前提下比较区间是否过宽。

5. 校准样本的划分必须以地块或空间区块为单位；禁止把同一时空邻域的像元随机对半切开分别充当校准与测试，否则覆盖率会被高估。

**定稿原则：** 业务对外只发布 \(\mu\) 与 \([\mu-z_{\alpha}s_{\alpha}\sigma,\ \mu+z_{\alpha}s_{\alpha}\sigma]\)；原始 \(\sigma\) 仅作中间量。95% 的 \(s_{0.95}\) 作为缺省尺度写入配置。

### 4.9 系统与介质

上述方法可固化为系统，包括：

- 立方构建模块；
- 模拟云监督模块；
- 特征门控模块；
- 时空变换器模块；
- 训练模块；
- 不确定性校准模块；
- 输出模块。

以及存储有计算机程序的非易失性介质，程序被处理器执行时实现上述步骤。

---

## 五、有益效果

相对第 2.3 节现有技术，本方案能够同时获得以下效果：

1. **与农情业务单元对齐。** 输出天然是「一块地、一个生长季、邻域格网」的 NDVI 立方，可直接对接地块统计、长势对比和保险勘验，而不必先做整景重建再裁切。
2. **监督不被云污染。** 模拟云只打在天然晴空真值上，网络学习的是可检验的填补能力；天然云区在训练中不进入损失，避免把 SCL 失败或薄云像元当成植被信号。
3. **空间与时间一并利用。** 令牌化使同一地块内邻域晴空像元、前后时相 SAR 与残存 NDVI 都能通过自注意力进入缺失位置，克服单点时序方法在连续缺测时的失效。
4. **特征冗余可被训练过程抑制。** 可微门控让 SAR 派生量与光学通道的权重服务最终重建误差；离线 Top-k 作为可替换实现，便于在算力或可解释性有硬约束时降级。
5. **填补结果带校准区间。** 经 PICP 对齐后的区间可用于「仅采纳高把握填补、低把握则回退到历史物候或人工判读」的业务策略，这是点估计方法不具备的。
6. **结构可消融、可部署。** B1–B4 把「邻域变换器 / 离线筛选 / 联合门控 / 不确定性」拆开，便于举证各技术特征的贡献；推理阶段门控为一次向量乘法，额外开销可忽略。

---

## 六、附图说明（交底用草稿）

实施专利申请文件时可请制图按下列逻辑出图。此处用文字框图代替正式附图。

**图1 总体流程图**  
地块矢量 → 多源立方 → 模拟云 → 门控 → 时空变换器 → 掩膜损失训练 → MC 采样 → 尺度校准 → NDVI 与区间输出。

**图2 地块邻域采样示意图**  
地块质心、\(P\times P\) 格网、10 m 像元、地理行列编号；标注「必须按坐标填格，禁止无序 reshape」。

**图3 模拟云监督示意图**  
同一 5×5 时相切片上：天然云、天然晴空、被挖掉的晴空（\(M_{eval}\)）、保留的 NDVI、全程保留的 SAR。

**图4 网络结构图**  
输入 \(X\) → 通道门控 \(g_c\) → 令牌嵌入 → 时间 PE / 空间 PE → Transformer × L → 回归头 → \(\hat{Y}\)。旁路标出 Dropout 位置。

**图5 不确定性校准示意图**  
横轴为尺度 \(s\)，纵轴为 PICP，水平线为名义置信度，交点为选用 \(s_{\alpha}\)；可另附校准前后覆盖率对照表。

**图6 消融结构图**  
B1 全通道、B2 离线 Top-k、B3 门控点估计、B4 门控 + MC + 校准。

---

## 七、具体实施方式

### 7.1 运行条件

- 数据：Sentinel-1 GRD IW VV/VH，Sentinel-2 SR B4/B8/SCL；地块 GeoJSON。
- 区域实施例：温带草地，质心约东经 23.3°、北纬 55.9° 一带；投影 EPSG:3346。
- 计算：Python + PyTorch；有 GPU 时在 GPU 训练，无 GPU 可 CPU 推理（MC 采样会变慢）。
- 地球引擎抽取时可按地块断点续写 HDF5，避免长任务中断。

### 7.2 关键参数一览

| 参数 | 实施例取值 | 调整原则 |
|---|---|---|
| 邻域边长 \(P\) | 5 | 增大可看更多邻域，令牌数按 \(P^{2}\) 上升；建议 3–11 |
| 地面分辨率 | 10 m | 与 Sentinel-2 对齐；30 m 传感器则窗口相应放大 |
| 合成步长 \(\Delta t\) | 6 日 | 宜覆盖 Sentinel-1 重访；过短导致空窗，过长抹平物候 |
| 时相数 \(T\) | 29 | 由生长季长度 / \(\Delta t\) 决定 |
| 特征通道 | VV, VH, CROSS, RATIO, NDVI | 可增入射角、轨道向、其它指数，门控维数随之改变 |
| 晴空 SCL | 4 或 5 | 水体、雪、云、云影一律视为非监督区 |
| 缺失标记 \(\tau\) | −100 | 必须远离 NDVI 物理范围 |
| 模拟云比例 \(r\) | 0.3 | 过低监督太少，过高使输入中 NDVI 几乎全缺 |
| 训练增强倍数 | 10 | 仅训练地块；验证为 1 |
| 嵌入维 \(d\) | 64 | 随样本量增减 |
| 层数 / 头数 | 2 / 4 | 邻域小时不必堆很深 |
| Dropout | 0.1 | 过小则 MC 方差不足，校准尺度 \(s\) 会偏大 |
| MC 次数 | 50 | 与推理时延折中 |
| Top-k（路径 A） | 3 | 随通道数调整 |
| 门控温度 | 1.0 | 可退火使门更硬 |
| NDVI 门控初值 | logit=+2 | 体现「光学优先」先验 |
| 校准搜索区间 | \(s\in[0.1,20]\) | 须覆盖 PICP 从低于到高于名义值 |

### 7.3 已知限制（交底时需向代理师说明，便于写说明书的「优选」而不把缺陷写成必要特征）

- 值域用 sigmoid 时，难以表达负 NDVI；说明书中应写成「值域约束模块，优选 [0,1]，亦可按应用选择」。
- 若空间填格错误，本方法退化为乱序集合上的时序模型，空间 PE 失效——故「按坐标对齐」应写入独立权利要求的必要技术特征，或至少写入优选实施例并强调。
- 仅用 Encoder Dropout 估计认知不确定度，不分离数据噪声；如需更细，可加异方差头作为扩展实施例。
- 跨年迁移（另一生长季立方）应作为说明书中的泛化实施例单独写，避免把「仅同年随机切分」写成唯一划分方式。

---

## 八、实施例

### 实施例 1（基线，对应 B1）

按第 4.3–4.4 节构建 2020 生长季地块立方，不做门控，全 5 通道进入时空变换器，掩膜 MSE 训练，单次前向输出 NDVI 点估计。用于证明「地块邻域令牌化 + 模拟云监督」本身相对单点插值的改进。

### 实施例 2（离线筛选，对应 B2）

在训练地块的晴空位置上计算五通道与 NDVI 的绝对相关，保留重要性最高的 3 通道，变换器输入维改为 3。其余同实施例 1。用于在特征维有硬限制（边缘计算、信道带宽）时的部署。

### 实施例 3（可微门控，对应 B3，优选）

五通道均保留，前置 sigmoid 门控，NDVI 通道 logit 初值为 +2。训练结束后可输出各通道门控权重，作为特征贡献的解释附件。点估计推理。

### 实施例 4（门控 + 不确定性校准，对应 B4，优选业务形态）

网络同实施例 3。推理时开启弃权，采样 50 次得到 \(\mu,\sigma\)。将验证地块按空间区块划出校准子集，对 80%/90%/95%/99% 分别搜索 \(s_{\alpha}\)，业务缺省使用 95% 尺度。输出填补 NDVI 与校准区间。

### 实施例 5（跨年应用）

用与实施例 1 相同的日历窗口抽取下一生长季立方（如 2021 年 4 月 9 日至 9 月 25 日），加载实施例 3 或 4 的权重做时间外推。用于证明方法不绑定某一特定年份的云型。

### 实施例 6（参数替换，不改变发明构思）

将 Sentinel-2 换为 Landsat；\(P=7\)；增加 VH/VV、雷达植被指数等通道；骨干换为同等令牌接口的 Conformer / 轻量 ViT；门控换为 Gumbel Top-k。只要仍满足第 4.1 节四条原则，即落入同一发明构思。

---

## 九、建议保护的技术要点（权利要求提纲）

以下供代理师起草独立/从属权利要求，不是正式权利要求书文本。

### 独立权利要求 1（方法）

一种地块级归一化植被指数云缺失填补方法，其特征在于，包括：

S1. 以地块位置为中心，按地理坐标将多源遥感观测填入规则网格，构建时间 × 空间邻域 × 多通道的数据立方；所述多通道至少包括微波后向散射特征与光学 NDVI，并记录天然晴空掩膜；

S2. 仅在天然晴空位置上生成模拟缺失掩膜，将对应位置的光学 NDVI 替换为缺失标记，微波特征保持不变；

S3. 对数据立方的通道维施加特征选择或可学习通道门控，得到加权或筛选后的特征；

S4. 将各时相、各邻域格点视为令牌，叠加时间位置编码与空间位置编码，经变换器编码后回归各格点各时相的 NDVI；

S5. 仅在模拟缺失掩膜为真的位置上计算预测与晴空真值的误差并更新参数。

### 独立权利要求 2（系统）

一种实现权利要求 1 所述方法的系统，包括立方构建模块、模拟缺失模块、通道门控模块、时空变换器模块和掩膜训练模块。

### 独立权利要求 3（介质）

一种计算机可读存储介质，其上存储的程序被执行时实现权利要求 1 所述步骤。

### 建议从属要点（可组合）

1. S1 中微波为 Sentinel-1 VV、VH 及由其计算的差和/或比；光学 NDVI 来自红光与近红外；晴空由场景分类层的植被或裸土等级确定。  
2. S1 中时间维为生长季等间隔合成，空间维为 \(P\times P\) 像元，填格必须依据像元地理坐标。  
3. S2 中模拟缺失比例为预设常数，训练地对同一立方多次抽样增强。  
4. S3 中门控为 \(g_c=\sigma(\ell_c/\tau_{temp})\)，与变换器联合训练；光学通道的 \(\ell\) 赋予高于微波通道的初值。  
5. S3 中离线按特征与 NDVI 的重要性排序后保留 Top-k。  
6. S4 中变换器为多层 Pre-Norm 多头自注意力，回归头带值域约束。  
7. 推理时保持弃权，多次前向得到均值与标准差。  
8. 在独立校准样本上搜索尺度因子，使经验覆盖率逼近名义置信度，再输出预测区间。  
9. 样本划分以地块或空间区块为单位，或采用不同年份立方做测试。

### 撰写时建议保留的区别特征（相对最接近现有技术）

代理师检索时，最接近对比文件可能是：像元级 SAR–光学深度学习填补，或整景 Transformer NDVI 重建。建议在独立权利要求中**至少同时保留**：

- 地块规则邻域 + 坐标对齐填格；
- 仅晴空真值上的模拟缺失监督；
- 通道门控（或筛选）与时空令牌变换器的串联。

不确定性校准可作为从属或第二条独立方法权利要求，以免独立权利要求被「不会做区间」的对比文件破坏新颖性时，整案没有退路。

---

## 十、替代方案与扩展（说明书应覆盖，防止规避）

1. 令牌可以是 \(2\times2\) 小子块而非单像元；注意力可改为时间轴与空间轴分解的轴向注意力以降低二次复杂度。  
2. 门控可作用在「时相」维（对无信息的空窗降权），与通道门控级联。  
3. 缺失标记可不采用极值常数，而采用可学习的缺失嵌入向量。  
4. 区间校准除全局标量 \(s\) 外，可按 NDVI 分档、按区域、按季节使用分段尺度。  
5. 骨干可替换为 ConvLSTM、3D CNN，只要输入仍是地块立方、损失仍在 \(M_{eval}\) 上。此类替换建议写入说明书，避免被「换骨干」轻易绕开；独立权利要求宜用「时空编码网络」上位，变换器作优选。

---

## 十一、复核与验收要点（便于发明人签字前自检）

1. 说明书中的 \(R^{2}\) 计算顺序是否写成「真值在前、预测在后」。  
2. 是否明确「无序 sample 后 reshape」不是本方案，坐标填格才是。  
3. 是否避免把「可微 RFE / 递归特征消除」写成必要名称——实施主路径是**通道门控**；若要用 RFE 一词，应在说明书中定义为「基于重要性的递归或软门控特征筛选家族」，并给出硬 RFE 与软门控两个实施例，避免因名称与实现不一致被审查意见质疑公开不充分。  
4. 独立权利要求是否同时包含地块立方、晴空模拟缺失、通道处理、时空编码四要素。  
5. 校准是否要求与测试地块分离。  
6. 数值实施例（6 日、5×5、29 步、五通道）是否都写成「优选」而非唯一。

---

## 十二、交底结论

本方案的技术核心是：**以地理对齐的地块邻域为样本，只在天然晴空真值上构造光学缺失监督，用可学习通道门控（或离线 Top-k）筛选雷达与光学特征，再以时空令牌变换器重建 NDVI；需要可靠性时，用同一网络的蒙特卡洛弃权方差经覆盖率尺度校准后输出预测区间。** 空间填格、损失位置与校准位置必须共用同一套晴空定义，禁止用云污染光学值当标签，也禁止把无序采样点当作空间 Patch。

---

（交底书完）
